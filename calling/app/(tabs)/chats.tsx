import React from 'react';
import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const chats = () => {
  return (
    <SafeAreaView className="bg-gray-500 flex-1 px-10">
      <View>
        <Text>chats</Text>
      </View>
    </SafeAreaView>
  );
};

export default chats;
